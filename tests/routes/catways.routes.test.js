// tests/routes/catways.routes.test.js
process.env.NODE_ENV = "test";

describe("Catways routes (placeholder)", () => {
  it("sanity check - placeholder", () => {
    expect(true).toBe(true);
  });
});
